#include "main.h"
#include <stdio.h>

/**
 * add - adds two numbers and return the value
 * @a: first number
 * @b: second number
 * Return: returns the value of the two added arguments
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
